# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-ts-nxp"
BBFILE_PATTERN_meta-ts-nxp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-ts-nxp = "6"

LAYERDEPENDS_meta-ts-nxp = "core"
LAYERSERIES_COMPAT_meta-ts-nxp = "walnascar"
